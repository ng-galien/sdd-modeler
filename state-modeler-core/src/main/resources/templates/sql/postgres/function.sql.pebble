{%- set tag = "$fn_" ~ function.name ~ "$" -%}
CREATE OR REPLACE FUNCTION {{ function.schema }}.{{ function.name }}({{ function.parameters | join(", ") }}) RETURNS {{ function.returnType }} AS {{ tag }}
{{ function.body }}{{ tag }} LANGUAGE {{ function.language }};
