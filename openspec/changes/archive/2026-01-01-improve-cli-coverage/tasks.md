## 1. Implementation
- [ ] 1.1 Add coverage requirement and goals for CLI module in spec.
- [ ] 1.2 Add/extend tests for `CliCommandHelpers` and `RepositoryMixin` covering invalid inputs and help messages.
- [ ] 1.3 Add test for `Main` entrypoint executing a representative command (e.g., `--help` or `list`).
- [ ] 1.4 Add tests for `util` paths (PathUtils) edge cases if needed to reach target.
- [ ] 1.5 Run test suite and ensure CLI package coverage >= 75% (jacoco) and `openspec validate`.
