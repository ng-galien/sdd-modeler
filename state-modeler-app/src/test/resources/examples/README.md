# Test fixtures

This folder contains YAML model fixtures used by automated tests and scripts.

Rules:

- These files are test fixtures and should be considered authoritative for tests.
- Do not edit them for documentation purposes. If editing is required for docs, copy to `scripts/examples`.
